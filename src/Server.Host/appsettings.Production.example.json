{
  "_comment": "Copy to appsettings.Production.json (git-ignored) on the server and fill in real values, or supply the same keys as environment variables, e.g. Postgres__AdminPassword.",
  "Server": {
    "BindAddress": "http://0.0.0.0:5000",
    "AllowedCorsOrigins": []
  },
  "Postgres": {
    "Host": "localhost",
    "Port": 5432,
    "AdminUsername": "postgres",
    "AdminPassword": "REPLACE_ME",
    "MasterDatabaseName": "reseller_system"
  }
}
